# Finance Backend

## Setup
1. Run: npm install
2. Run: node server.js

## APIs
- POST /users
- GET /users
- POST /records (admin only)
- GET /records
- GET /dashboard

## Note
Role is passed via headers:
role: admin | analyst | viewer
